[project]
name = "ai-scraper"
version = "0.1.0"
description = "Yet Another AI Scraper"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pydantic>=2.10.6",
    "httpx>=0.28.1",
    "anthropic>=0.45.0",
    "python-dotenv>=1.0.0",
    "beautifulsoup4>=4.12.0",
]

[dependency-groups]
dev = [
    "ruff>=0.9.6",
]
